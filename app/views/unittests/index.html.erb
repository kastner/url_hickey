<h1>Listing unittests</h1>

<div>
  <% for unittest in @unittests %>
    Hi, here is the full url for UnitTest #<%= unittest.id %>: 
    <%= unittest.full_url %>
  <% end %>  
</div>

<%= link_to 'New unittest', new_unittest_path %>
